name: Daily FreeNYC Crawler

on:
  schedule:
    - cron: '0 13 * * *'  # Runs every day at 9:00 AM New York time (UTC+4)
  workflow_dispatch:  # Allows manual triggering

jobs:
  run-crawler:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.10

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run skint_crawler.py
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          python skint_crawler.py

#      - name: Upload output files as artifact
#        uses: actions/upload-artifact@v3
#        with:
#          name: freenyc-events-${{ github.run_number }}
#          path: output/
